% ---------------------------------------------------------------------------
% IEEE conference build.  Compile with:
%     tectonic -X compile main.tex --outdir .
% The body is shared verbatim with ../springer/main.tex; only this wrapper
% differs.
% ---------------------------------------------------------------------------
\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{siunitx}
\usepackage{microtype}
\usepackage{cite}
\usepackage[hidelinks]{hyperref}

\graphicspath{{../shared/generated/}{../shared/diagrams/}}

% siunitx v3 dropped \degree; every use in the body is in math mode.
\providecommand{\degree}{^{\circ}}

% IEEEtran is two-column: a "wide" float must span both columns, or the
% 9-column results tables and the small-multiple figure are illegible.
\newenvironment{wfigure}{\csname figure*\endcsname}{\csname endfigure*\endcsname}
\newenvironment{wtable}{\csname table*\endcsname}{\csname endtable*\endcsname}
\newcommand{\tabscale}{\scriptsize}

% IEEEtran appends its own colon to a run-in \paragraph; adding a period too
% renders as "Delta targets.:".
\newcommand{\para}[1]{\paragraph{#1}}

% The shared body uses natbib's \citep; IEEEtran uses plain \cite.
\providecommand{\citep}[1]{\cite{#1}}

% Every experimental number in the prose is a macro generated by
% backend/scripts/build_paper_assets.py.
\input{../shared/generated/macros}

\begin{document}

\title{Rising Skill, Falling Skill: Why a Single Baseline Cannot Tell You
Whether an Ocean Forecast Model Has Learned Anything}

\author{\IEEEauthorblockN{Deepak Krishna}
\IEEEauthorblockA{\textit{MarisAI} \\
India \\
deepakkrishna2206@gmail.com}}

\maketitle

\begin{abstract}
Statistical forecasts of ocean state are conventionally scored against
\emph{persistence}, the last observed value. We show this single baseline is
not merely incomplete but actively misleading, because it fails in a direction
that flatters the model. Persistence decays with forecast horizon while a
seasonal cycle does not, so the persistence-referenced score of a model that
has learned nothing but the time of year stays flat, or rises, while the model
genuinely degrades. We audit \NVars{} operational ocean variables across
\NSites{} regime-stratified sites and four horizons
(\SIrange{1}{30}{\day}), using one gradient-boosted framework with no
variable-specific code, and score every model against both persistence and a
leakage-controlled day-of-year climatology. Median skill against persistence is
\MedSkillPOne{} at \SI{1}{\day} and \MedSkillPThirty{} at \SI{30}{\day}
--- essentially unchanged, which reads as a horizon-invariant forecaster. Over
the same range median skill against climatology falls monotonically from
\MedSkillCOne{} to \MedSkillCThirty{}, declining with horizon for
\NFallingClim{} of \NVars{} variables without exception; by \SI{30}{\day}
\NLoseClimThirty{} of \NVars{} variables lose to the seasonal cycle outright,
against \NLosePersThirty{} that lose to persistence. The two baselines
therefore support opposite conclusions about the same predictions, and
\NLoseToClim{} model--horizon combinations beat persistence while losing to
climatology. A three-arm feature ablation then shows that the natural
explanation for those cases is wrong: \NFlatteredNonSeasonal{} of the
\NFlattered{} retain their skill when every calendar feature is removed, while
the most seasonally dependent variable in the set comfortably \emph{beats}
climatology. Losing to climatology therefore reports the baseline's strength,
not the model's mechanism; diagnosing the latter needs an ablation. We further
test whether these pooled models transfer to locations they never saw, using
leave-one-site-out validation blocked in space and time simultaneously
(\NLosoFits{} held-out fits). Transfer is only partial: median held-out skill
at \SI{7}{\day} falls to \MedLosoSeven{} from \MedPooledSeven{} pooled --- a
roughly threefold drop --- with \LosoPositiveShare{}\% of held-out sites
retaining positive skill. We report
\NLoseToPers{} model--horizon combinations that lose to persistence, and argue
that reporting both baselines across multiple horizons should be a minimum
standard for this class of work.
\end{abstract}

\begin{IEEEkeywords}
ocean forecasting, forecast verification, skill scores, baseline selection,
spatial cross-validation, gradient boosting
\end{IEEEkeywords}

\input{../shared/body}

\section*{Reproducibility}
Experiments run from a pinned archival snapshot (\AsOf{}). The climatology
implementation, its leakage tests, the experiment driver and the asset
generator that produces every figure and table are included in the
accompanying source tree (\texttt{forecasting/climatology.py},
\texttt{tests/test\_climatology.py},
\texttt{scripts/run\_paper\_experiments.py},
\texttt{scripts/build\_paper\_assets.py}).

\bibliographystyle{IEEEtran}
\bibliography{../shared/references}

\end{document}
