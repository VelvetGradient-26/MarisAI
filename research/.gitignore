# Tectonic intermediates. The compiled PDFs under papers/ are deliberately NOT
# ignored — they are the deliverable, and a reader should not need a LaTeX
# toolchain to read the paper.
*.aux
*.log
*.out
*.xdv
*.bbl
*.blg
*.toc

# Jupyter
.ipynb_checkpoints/
**/.ipynb_checkpoints/
